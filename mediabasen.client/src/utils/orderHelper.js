const orderPageQueries = {
  filter: "filter",
  page: "page",
};

const orderFilterOptions = {
  all: "all",
  shipped: "shipped",
  unshipped: "unshipped",
  unpaid: "unpaid",
};

export default { orderPageQueries, orderFilterOptions };
